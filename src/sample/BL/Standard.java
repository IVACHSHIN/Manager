package sample.BL;

public class Standard extends Person {
    public Standard(int id, String name,
                    String lastname, int year,
                    int dok, int tel, String email,
                    String tariff, int price) {
        super(id, name, lastname, year,
                dok, tel, email, tariff, price);
    }
}

